import { StyleSheet } from "react-native";

import { palette } from "./colors";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    padding: 16,
    width: "100%",
    height: "100%",
  },
});
